<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="com.mycompany.mapper.PestMapper">
	<!-- <select id="boardList" resultType="kr.smhrd.domain.Board">
		select * from board order by idx desc ? 대신에 #{}를 쓴다
	</select>
	<insert id="boardInsert" parameterType="kr.smhrd.domain.Board">
		insert into board(title, contents, writer)
		values(#{title}, #{contents}, #{writer})
	</insert>
	<select id="boardContent" resultType="kr.smhrd.domain.Board"
		parameterType="int"> parameterType이 하나일때는 생략 가능
		select * from board where idx = #{idx}
	</select>
	<delete id="boardDelete" parameterType="int">
		Delete from board where idx = #{idx}
	</delete>
	<update id="boardUpdate" parameterType="kr.smhrd.domain.Board">
		Update board set title=#{title}, contents=#{contents} where idx=#{idx}
	</update> -->
</mapper> 